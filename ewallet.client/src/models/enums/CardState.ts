export type CardState = 'Active' | 'Inactive' | 'Disabled' | 'Expired'

export const CardState = {
    Active: 'Active' as CardState,
    Inactive: 'Inactive' as CardState,
    Disabled: 'Disabled' as CardState,
    Expired: 'Expired' as CardState
};